# Journal des modifications — DIARRA

Ce journal est spécifique au projet DIARRA (`c:\Users\Admin\DIARRA\diarra`). Il n'est jamais mélangé avec un autre projet.

Une ligne = une modification. Jamais de résumé groupé. Chaque entrée est ajoutée immédiatement après la modification correspondante, jamais différée en fin de session.

**Statut** : `Local (non déployé)` = existe seulement sur cette machine en dev · `Déployé` = poussé en prod/serveur.
**Origine** : `Automatique` = écrit par l'IA sans saisie manuelle · `Manuel` = écrit à la main par le développeur. Jamais vide.
**Type** : nature du changement ; si sensible/structurant (sécurité, base de données, paiement, déploiement), le préciser explicitement ici.

| Date | Projet | Fichier(s) | Modification | Statut | Type | Origine | Reproduire manuellement |
|------|--------|-----------|---------------|--------|------|---------|--------------------------|
| 2026-09-02 | DIARRA | backend/migrations/026_sale_reminders_manual_ops.sql | Nouvelle migration : colonnes `sales.reminded_at`/`reminder_count`/`manually_confirmed_by`/`manually_confirmed_at`, `payouts.is_manual`/`manual_note`/`fee_cfa`/`settled_by`, index partiel `idx_sales_pending_reminder` | Local (non déployé) | Base de données (structurant) | Automatique | Créer le fichier SQL puis `go run ./cmd/migrate` dans backend/ |
| 2026-09-02 | DIARRA | backend/internal/email/notifications.go | Ajout `SendCartReminder` : email de relance « Vous n'avez pas terminé votre achat » avec lien de reprise de paiement (checkout_token) | Local (non déployé) | Fonctionnalité (email) | Automatique | Ajouter la méthode sur NotificationService, même gabarit que SendOrderConfirmed |
| 2026-09-02 | DIARRA | backend/internal/repository/sale_repo.go, model/sale.go | `PendingSaleView` (contact acheteur complet), `ListPendingAndFailed`, `FindPendingView(ForVendor)`, `MarkReminded`, `ListRemindable` (cadence 1h/24h), `MarkManuallyConfirmed` | Local (non déployé) | Fonctionnalité (DB) | Automatique | Ajouter méthodes repo + champs RemindedAt/ReminderCount sur model.Sale |
| 2026-09-02 | DIARRA | backend/internal/repository/payout_repo.go, model/payout.go | `SettleManually`, `CreateManual` (provider='manual', status='paid'), champs is_manual/manual_note/fee_cfa dans payoutColumns + ListAllAdmin | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter méthodes repo + champs sur model.Payout, MAJ des SELECT explicites |
| 2026-09-02 | DIARRA | backend/internal/handler/sale_handler.go | `RemindVendor` (POST /api/vendor/sales/{id}/remind), helper `sendPendingReminder`, boucle `RunReminderLoop` (ticker 15 min, relance auto 1h puis 24h) | Local (non déployé) | Fonctionnalité | Automatique | Ajouter handlers + lancer `go saleHandler.RunReminderLoop(ctx)` dans main.go |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `PendingSales`, `RemindSale`, `MarkSalePaid` (+ livraison email/fichier), `SettlePayoutManual`, `CreateManualPayout` | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter handlers sur AdminHandler, imports mime/path/filepath |
| 2026-09-02 | DIARRA | backend/cmd/server/main.go | Routes : /api/vendor/sales/{id}/remind ; /api/admin/sales/pending, /sales/{id}/remind, /sales/{id}/mark-paid, /payouts/{id}/settle-manual, /payouts/manual ; lancement RunReminderLoop | Local (non déployé) | Déploiement (routes) | Automatique | Ajouter les r.Post/r.Get dans les groupes existants + go RunReminderLoop |
| 2026-09-02 | DIARRA | frontend/src/lib/api.ts | Méthodes : remindVendorSale, getPendingSales, remindSale, markSalePaid, settlePayoutManual, createManualPayout | Local (non déployé) | Fonctionnalité (API client) | Automatique | Ajouter les entrées dans l'objet api |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/vendor/sales/page.tsx | Colonne téléphone acheteur + bouton « Relancer » sur les ventes en attente (mobile + desktop) | Local (non déployé) | Fonctionnalité (UI) | Automatique | Ajouter handleRemind + colonnes/boutons |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/pending-sales/page.tsx | Nouvelle page : liste pending/failed avec contact acheteur, bouton Relancer + Confirmer le paiement (ConfirmDialog) | Local (non déployé) | Fonctionnalité (UI) | Automatique | Créer la page, lien depuis le dashboard admin |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/payouts/page.tsx | Nouvelle page : tous les versements, relance échec, « Marquer payé (manuel) » avec note+frais, formulaire création versement manuel | Local (non déployé) | Paiement (UI) | Automatique | Créer la page, lien depuis le dashboard admin |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/page.tsx | Dashboard : liens « Paiements en attente & échoués » et « Versements (manuels inclus) » | Local (non déployé) | UI | Automatique | Ajouter 2 <Link> dans « Actions rapides » |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/users/page.tsx | Colonne « Téléphone » dans la liste des utilisateurs | Local (non déployé) | UI | Automatique | Ajouter champ phone à l'interface User + colonne |
| 2026-09-02 | DIARRA | backend/internal/payment/pawapay.go | **CORRECTIF RACINE incident paiements** : ajout du champ `callbackUrl` à `PaymentPageRequest` + méthode `CallbackURL()`. Sans ce champ, PawaPay ne poussait JAMAIS le webhook de statut → toutes les ventes bloquées « pending » malgré paiement réussi (aucune vente confirmée depuis le 18/08) | Local (non déployé) | Paiement (structurant, correctif critique) | Automatique | Ajouter le champ JSON `callbackUrl,omitempty` + getter sur PawaPayClient |
| 2026-09-02 | DIARRA | backend/internal/handler/sale_handler.go | `initiatePaymentPage` passe désormais `CallbackUrl: h.pawapay.CallbackURL()` (PAWAPAY_CALLBACK_URL) dans la requête de page de paiement | Local (non déployé) | Paiement (structurant) | Automatique | Renseigner le champ CallbackUrl dans la construction de PaymentPageRequest |
| 2026-09-02 | DIARRA | backend/internal/handler/webhook_handler.go | Extraction `ConfirmPaidSale` (statut→paid + emails acheteur/vendeur + notifs in-app + cagnotte + invalidation cache) partagée par webhook/job/bouton admin. Nouveau job `RunDepositReconcileLoop` (ticker 10 min) : revérifie via l'API PawaPay les ventes « pending » PawaPay de < 3 j et applique COMPLETED→paid / FAILED→failed — filet de sécurité si un webhook se perd | Local (non déployé) | Paiement (structurant) | Automatique | Extraire la logique COMPLETED du PawaPayWebhook en méthode, ajouter la boucle ticker + `go webhookHandler.RunDepositReconcileLoop(ctx)` dans main.go |
| 2026-09-02 | DIARRA | backend/internal/repository/sale_repo.go | `ListPendingForProvider(provider, maxAge)` : ventes « pending » d'un prestataire créées il y a moins de maxAge (pour le job de réconciliation) | Local (non déployé) | Fonctionnalité (DB) | Automatique | Ajouter la méthode repo (SELECT saleColumns + filtre status/provider/created_at) |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `CheckSaleProvider` (POST /api/admin/sales/{id}/check-provider) : interroge PawaPay en direct sur le vrai statut du dépôt, persiste le provider_transaction_id, et confirme la vente (via `webhook.ConfirmPaidSale`) si COMPLETED / la passe failed si FAILED. `AdminHandler` reçoit `*WebhookHandler` en dépendance | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le champ webhook + param constructeur, le handler, la route, passer webhookHandler dans NewAdminHandler |
| 2026-09-02 | DIARRA | backend/cmd/server/main.go | Route /api/admin/sales/{id}/check-provider ; `NewAdminHandler(..., webhookHandler)` ; `go webhookHandler.RunDepositReconcileLoop(ctx)` | Local (non déployé) | Déploiement (routes) | Automatique | Ajouter r.Post + argument + goroutine |
| 2026-09-02 | DIARRA | frontend/src/lib/api.ts | Méthode `checkSaleProvider(id)` → POST /api/admin/sales/{id}/check-provider | Local (non déployé) | Fonctionnalité (API client) | Automatique | Ajouter l'entrée dans l'objet api |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/pending-sales/page.tsx | Bouton « Vérifier chez PawaPay » sur chaque vente non-KPay : appelle checkSaleProvider, affiche le statut PawaPay traduit, recharge la liste. Table `PROVIDER_STATUS_LABELS` | Local (non déployé) | Fonctionnalité (UI) | Automatique | Ajouter handleCheckProvider + le bouton dans la cellule Actions |
| 2026-09-02 | DIARRA | backend/internal/payment/country.go | Nouveau fichier : `CountryFromPhone` (indicatif tél → ISO3, dérivé de XOFOperators.DialCode), `CountryName` (20 pays), `CountryLabel`. Base du classement des utilisateurs par pays (le modèle User n'a pas de champ pays) | Local (non déployé) | Fonctionnalité | Automatique | Créer le fichier avec la map indicatif→ISO3 issue de XOFOperators |
| 2026-09-02 | DIARRA | backend/internal/model/settings.go | Clés `whatsapp_community_url` (général) + `WhatsAppCommunitySettingKey(iso3)` → `whatsapp_community_url_<iso3>` (lien communauté WhatsApp par pays, fallback général) | Local (non déployé) | Fonctionnalité | Automatique | Ajouter la constante + la fonction de clé, même schéma que CheckoutProviderSettingKey |
| 2026-09-02 | DIARRA | backend/internal/email/notifications.go | `WhatsAppCommunityHTML(link, countryLabel, hasCountry)` (bloc « Rejoindre la communauté WhatsApp », bouton vert #25D366). `SendWelcome` (signature étendue) + nouveau `SendVendorWelcome` + `SendAdminMessageWithCommunity` : tous concatènent le bloc si un lien est fourni | Local (non déployé) | Fonctionnalité (email) | Automatique | Ajouter le helper HTML + les nouvelles signatures ; SendAdminMessage délègue à SendAdminMessageWithCommunity |
| 2026-09-02 | DIARRA | backend/internal/service/auth_service.go | `AuthService` reçoit `*SettingsRepo`. Sur `AddRole` avec rôle vendeur → `sendVendorWelcome` en tâche de fond : email « Bienvenue espace vendeur » avec le lien WhatsApp du pays du vendeur (déduit du tél), fallback général | Local (non déployé) | Fonctionnalité (email) | Automatique | Ajouter settingsRepo au constructeur + le hook dans AddRole |
| 2026-09-02 | DIARRA | backend/internal/repository/user_repo.go | `UserWithStats.Country`/`CountryLabel` (dérivés côté handler). `ListEmailsWithPhone` (+ `EmailWithPhone`) pour la diffusion ciblée par pays | Local (non déployé) | Fonctionnalité (DB) | Automatique | Ajouter les champs + la méthode SELECT email, phone |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `Users` accepte `?country=ISO3\|UNKNOWN` (pays déduit du tél). Nouveau `UsersCountrySummary` (GET /api/admin/users/by-country : répartition par pays). `SendBroadcast` accepte `country` (filtre destinataires) + ajoute le bloc WhatsApp (lien du pays ciblé, ou par destinataire). `SendUserMessage` ajoute le bloc WhatsApp du pays du destinataire. `whatsappCommunityBlock` helper. `UpdateSettings` valide `whatsapp_community_url*` (vide ou http(s)) | Local (non déployé) | Fonctionnalité | Automatique | Étendre Users + SendBroadcast + SendUserMessage, ajouter UsersCountrySummary et la route, import sort |
| 2026-09-02 | DIARRA | backend/cmd/server/main.go | Route GET /api/admin/users/by-country ; `NewAuthService(..., settingsRepo)` | Local (non déployé) | Déploiement (routes) | Automatique | Ajouter r.Get + l'argument |
| 2026-09-02 | DIARRA | frontend/src/lib/api.ts | `getUsers(country?)`, `getUsersByCountry()`, `sendBroadcast` accepte `country` | Local (non déployé) | Fonctionnalité (API client) | Automatique | Étendre les 3 entrées |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/users/page.tsx | Colonne « Téléphone / Pays » avec lien wa.me cliquable + libellé pays. Filtre pays (options = répartition /by-country). Bouton + modale « Message groupé par pays » (sujet + HTML, sélecteur pays, bouton Test, appel sendBroadcast avec country) | Local (non déployé) | Fonctionnalité (UI) | Automatique | Ajouter interface Country, waLink, états group*, filtre pays, modale |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/settings/page.tsx | Carte « Communauté WhatsApp » : lien général + un lien par pays checkout (clés whatsapp_community_url / whatsapp_community_url_<iso3>) | Local (non déployé) | Fonctionnalité (UI) | Automatique | Ajouter whatsappLinks state, chargement/sauvegarde, la carte |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `SetRole` (passage vendeur décidé par un admin) envoie aussi l'email de bienvenue vendeur + lien WhatsApp du pays, via nouveau helper `AdminHandler.sendVendorWelcome` (miroir de AuthService.sendVendorWelcome). Pas de double envoi : SetRole et AuthService.AddRole sont mutuellement exclusifs | Local (non déployé) | Fonctionnalité (email) | Automatique | Ajouter le helper + le hook `go h.sendVendorWelcome` dans SetRole sur grant+vendeur |
| 2026-09-02 | DIARRA | backend/internal/handler/payout_handler.go | `Create` (POST /api/vendor/payouts) NE déclenche plus aucun versement : la demande est seulement enregistrée `requested` (en attente). Plus d'appel PawaPay/KPay, plus d'erreur `payout_rejected`/`gateway_disabled` renvoyée au vendeur. Retrait de l'import `fmt` | Local (non déployé) | Paiement (structurant) | Automatique | Supprimer tout le bloc `switch providerName` de Create, ne garder que payoutRepo.Create + cache.Del |
| 2026-09-02 | DIARRA | backend/cmd/server/main.go | `POST /api/vendor/payouts` n'a plus `RequireVerifiedPhone` (la validation du numéro se fait à l'enregistrement du moyen de versement, SetPayoutMethod). Nouvelles routes admin : `/payouts/{id}/settle-auto`, `/payouts/{id}/check-provider`. Lancement `go webhookHandler.RunPayoutReconcileLoop` | Local (non déployé) | Paiement (structurant, routes) | Automatique | Retirer le middleware sur POST /payouts, ajouter les 2 r.Post + la goroutine |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `initiatePayoutWithProvider` (helper extrait de RetryPayout : déclenche PawaPay/KPay, marque failed+cache si rejet). `SettlePayoutAuto` (POST /payouts/{id}/settle-auto : règle une demande `requested` automatiquement). `CheckPayoutProvider` (POST /payouts/{id}/check-provider : vérifie le vrai statut, applique paid+email vendeur / failed). `RetryPayout` réécrit sur le helper | Local (non déployé) | Paiement (structurant) | Automatique | Extraire le helper, ajouter les 2 handlers, réécrire RetryPayout |
| 2026-09-02 | DIARRA | backend/internal/repository/payout_repo.go | `ListProcessing(maxAge)` : versements `processing` avec provider_reference, pour le job de réconciliation. Import `time` | Local (non déployé) | Fonctionnalité (DB) | Automatique | Ajouter la méthode (SELECT payoutColumns + filtre status/ref/requested_at) |
| 2026-09-02 | DIARRA | backend/internal/handler/webhook_handler.go | `notifyAdminsPayoutFailed` (notif in-app aux admins ; le vendeur n'est JAMAIS notifié d'un échec de versement). Webhooks payout PawaPay+KPay : suppression de la notif vendeur `payout_failed`, ajout cache.Del + notifyAdminsPayoutFailed. Nouveau job `RunPayoutReconcileLoop` (10 min, versements `processing` < 3j revérifiés via API → paid/failed) | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le helper + la boucle, retirer les 2 `notify(payout_failed)` vendeur |
| 2026-09-02 | DIARRA | frontend/src/lib/api.ts | `settlePayoutAuto(id)`, `checkPayoutProvider(id)` | Local (non déployé) | API client | Automatique | Ajouter les 2 entrées |
| 2026-09-02 | DIARRA | frontend/src/lib/constants.ts | `PAYOUT_STATUS_LABELS.requested` : « Demandé » → « En attente » (libellés = vue admin ; le vendeur remappe) | Local (non déployé) | UI (libellé) | Automatique | Changer la valeur |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/vendor/earnings/page.tsx | Le vendeur ne voit QUE « Versé » (paid) ou « En cours de traitement » (tout le reste, échec compris) — helpers `vendorPayout*`. Plus jamais de `failure_reason` ni de badge rouge. « sous 48h » → « sous 24h ouvrées ». Le tiroir de retrait affiche le moyen de versement (opérateur + numéro masqué) + message rassurant | Local (non déployé) | UI (paiement) | Automatique | Ajouter les helpers, remplacer les rendus de statut, MAJ textes |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/payouts/page.tsx | Boutons par demande : « Régler via <provider> » (sur `requested` → settlePayoutAuto, ConfirmDialog), « Vérifier chez <provider> » (sur `processing` → checkPayoutProvider), « Relancer » (failed), « Marquer payé (manuel) » (existant). Filtre : « Demandé » → « En attente (à traiter) » | Local (non déployé) | UI (paiement) | Automatique | Ajouter handleSettleAuto/handleCheckProvider + les boutons + le ConfirmDialog auto |
| 2026-09-02 | DIARRA | backend/internal/repository/payout_repo.go | `PayoutWithUser` enrichi de `VendorPayoutPhone/Operator/Country` (repli `users.payout_*`) ; `ListAllAdmin` fait désormais le SELECT sur `users.payout_phone/operator/country` en plus de l'email | Local (non déployé) | Fonctionnalité (DB) | Automatique | Ajouter les 3 champs + colonnes au SELECT/Scan de ListAllAdmin |
| 2026-09-02 | DIARRA | backend/internal/handler/admin_handler.go | `CreateManualPayout` : si `phone` est vide dans la requête, repli sur `users.payout_phone` du vendeur (via `GetPayoutMethod`) pour la traçabilité comptable | Local (non déployé) | Paiement | Automatique | Ajouter le fallback avant l'appel à `payoutRepo.CreateManual` |
| 2026-09-02 | DIARRA | frontend/src/lib/payout-receipt.ts | `ReceiptPayout` + `fee_cfa`/`is_manual`/`manual_note` ; le reçu affiche désormais le vrai moyen de paiement, les frais réels et le net = brut − frais (au lieu de « Frais 0 FCFA » figé) | Local (non déployé) | UI (paiement) | Automatique | Ajouter les champs + recalcul `net` dans openPayoutReceipt |
| 2026-09-02 | DIARRA | frontend/src/app/(app)/admin/payouts/page.tsx | Colonne « Moyen de paiement » (opérateur + numéro, repli sur le moyen enregistré par le vendeur si le versement n'en a pas, sinon « — non renseigné » + lien fiche vendeur) ; bouton « Reçu » par ligne (openPayoutReceipt) ; encadré « Où envoyer l'argent » dans la modale de règlement manuel | Local (non déployé) | UI (paiement, traçabilité) | Automatique | Ajouter `paymentMethodOf`, la colonne, le bouton Reçu, l'encadré dans la modale settleTarget |
| 2026-09-02 | DIARRA | backend/internal/repository/sale_repo.go | Correctif bug préexistant (pas lié aux changements du jour) : `SaleRepo.SalesByDay` faisait `now() - ($1 \|\| ' days')::interval` — pgx lie `$1` en `int4`, Postgres refuse `integer \|\| unknown` → `/api/admin/analytics` renvoyait 500 en prod. Remplacé par `make_interval(days => $1)` | Déployé | Correctif (bug) | Automatique | Remplacer l'expression d'intervalle dans la requête SQL de SalesByDay |
| 2026-09-03 | DIARRA | backend/internal/handler/sale_handler.go | Verrou KPay checkout : `resolveCheckoutProvider` ignore un réglage `checkout_provider_<pays>` = "kpay" (retombe sur "pawapay"). Carte/PayPal → provider "paypal" (déjà en place). | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le `if provider == "kpay" { return "pawapay" }` après le Get settings |
| 2026-09-03 | DIARRA | backend/internal/handler/admin_handler.go | `UpdateSettings` : refuse désormais "kpay" pour `gateway_op_*` (versements) ET `checkout_provider_*` (checkout) — seuls off/pawapay et pawapay acceptés. Suppression du bloc de validation KPayProviderCodes. | Local (non déployé) | Paiement (structurant) | Automatique | Retirer "kpay" des valeurs acceptées dans les 2 case du switch |
| 2026-09-03 | DIARRA | backend/internal/handler/payout_handler.go | `resolvePayoutProvider` : une valeur "kpay" restée en base est traitée comme "pawapay". | Local (non déployé) | Paiement | Automatique | Ajouter `if v == "kpay" { return "pawapay" }` |
| 2026-09-03 | DIARRA | backend/cmd/server/main.go | KPay SUSPENDU : client jamais instancié (`kpay` reste nil), routes webhook /api/webhooks/kpay* retirées. Construction du client PayPal (PAYPAL_CLIENT_ID/SECRET/BASE_URL/WEBHOOK_ID) + passage `paypal` à NewSaleHandler/NewWebhookHandler/NewAdminHandler. Route POST /api/webhooks/paypal. | Local (non déployé) | Paiement + déploiement (structurant) | Automatique | Remplacer le bloc `if KPAY_API_KEY...` par un log INFO ; ajouter le bloc `var paypal` symétrique ; 3 constructeurs + 1 route |
| 2026-09-03 | DIARRA | backend/internal/handler/webhook_handler.go | `PayPalWebhook` : ajout du case `PAYMENT.CAPTURE.DECLINED` → vente pending passée en failed + email/notif acheteur. | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le case avant `PAYMENT.CAPTURE.REFUNDED`, même extraction saleID |
| 2026-09-03 | DIARRA | frontend/src/app/checkout/checkout-view.tsx | `CARD_PAYMENT_ENABLED` false → true : l'option « Carte bancaire / PayPal » réapparaît au checkout (redirige vers PayPal). | Local (non déployé) | Paiement (UI) | Automatique | Passer la constante à true |
| 2026-09-03 | DIARRA | frontend/src/app/(app)/admin/settings/page.tsx | KPay retiré des 2 panneaux : « Versements vendeur — par opérateur » (boutons off/pawapay seulement) et « Paiement à l'achat — par pays » (PawaPay figé, non modifiable). Sauvegarde force toujours "pawapay" pour checkout_provider_*. Suppression de `KPAY_SUPPORTED`. Avertissement ambre si une valeur "kpay" traîne en base. | Local (non déployé) | Paiement (UI) | Automatique | Retirer 'kpay' des .map de boutons, la Set KPAY_SUPPORTED, forcer 'pawapay' à la sauvegarde checkout, MAJ CardDescription |
| 2026-09-03 | DIARRA | backend/migrations/027_paypal_payouts.sql | Nouvelle migration : `users.payout_paypal_email`, `payouts.paypal_email` + `payouts.paypal_batch_id` (versements vendeur via PayPal Payouts API v1) | Local (non déployé) | Base de données (structurant) | Automatique | Créer le fichier SQL puis `go run ./cmd/migrate` |
| 2026-09-03 | DIARRA | backend/internal/payment/paypal.go | `SendPayout` (POST /v1/payments/payouts, 1 item/lot, montant FCFA→USD) + `GetPayoutBatch` (GET statut du lot) + types PayPalPayout{Request,Response,Status} | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter les 2 méthodes + types sur PayPalClient |
| 2026-09-03 | DIARRA | backend/internal/payment/provider.go | `PayoutOp` gagne `Email`+`AmountXOF` (PayPal). `paypalAdapter.InitiatePayout`/`GetPayoutStatus` implémentés (au lieu du stub) + `normalizePayPalPayoutStatus` | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter les 2 champs, remplacer les 2 stubs, ajouter le normaliseur |
| 2026-09-03 | DIARRA | backend/internal/repository/payout_repo.go | payoutColumns + `paypal_email`,`paypal_batch_id`. `CreatePayPal(userID, amount, email)` (provider='paypal'), `SetPayPalBatchID` (batch id + status processing). ListAllAdmin : SELECT `p.paypal_email`, `u.payout_paypal_email` en plus | Local (non déployé) | Paiement (DB) | Automatique | Étendre les colonnes/scan, ajouter les 2 méthodes, MAJ ListAllAdmin |
| 2026-09-03 | DIARRA | backend/internal/repository/user_repo.go | `GetPayoutPayPalEmail` / `SetPayoutPayPalEmail` (email PayPal du vendeur, coexiste avec le mobile money) | Local (non déployé) | Paiement (DB) | Automatique | Ajouter les 2 méthodes (SELECT/UPDATE users.payout_paypal_email) |
| 2026-09-03 | DIARRA | backend/internal/model/payout.go | `Payout` + `PayPalEmail`/`PayPalBatchID`. `SetPayoutMethodInput` + `Channel`+`PayPalEmail`. Constante `PayPalPayoutMinCFA = 600` (~1 USD) | Local (non déployé) | Paiement | Automatique | Étendre les 2 structs + ajouter la constante |
| 2026-09-03 | DIARRA | backend/internal/handler/payout_handler.go | `GetPayoutMethod` renvoie `active_channel`+`paypal_email`. `SetPayoutMethod` : branche `channel=="paypal"` (valide l'email via `looksLikeEmail`, n'écrase pas le mobile money) sinon flux mobile money. `Create` : PayPal prioritaire si email présent (sinon mobile money) ; plancher PayPalPayoutMinCFA (repli mobile money si sous le seuil et MM dispo). Import `strings` | Local (non déployé) | Paiement (structurant) | Automatique | Étendre les 3 handlers + ajouter looksLikeEmail |
| 2026-09-03 | DIARRA | backend/internal/handler/admin_handler.go | `initiatePayoutWithProvider` gère `provider=="paypal"` (SendPayout via l'adaptateur, SetPayPalBatchID). `CheckPayoutProvider` interroge PayPal (GetPayoutBatch) et mappe completed/failed→COMPLETED/FAILED | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le bloc paypal en tête de initiatePayoutWithProvider + le case paypal dans CheckPayoutProvider |
| 2026-09-03 | DIARRA | backend/internal/handler/webhook_handler.go | `PayPalWebhook` gère `PAYMENT.PAYOUTS-ITEM.SUCCEEDED/FAILED/BLOCKED/RETURNED/DENIED` (sender_item_id = ID versement DIARRA → paid/failed + email vendeur / notif admins). `reconcilePayoutsPass` : case `paypal` (GetPayoutBatch) | Local (non déployé) | Paiement (structurant) | Automatique | Ajouter le case payout-item avant le switch existant + le case paypal dans reconcilePayoutsPass |
| 2026-09-03 | DIARRA | frontend/src/lib/api.ts | `getPayoutMethod`/`getAccountPayoutMethod` renvoient active_channel+paypal_email. `setPayoutMethod`/`setAccountPayoutMethod` : union `{channel:'mobile_money',phone,operator,country}` | `{channel:'paypal',paypal_email}` | Local (non déployé) | API client | Automatique | Réécrire les 4 signatures |
| 2026-09-03 | DIARRA | frontend/src/app/(app)/account/page.tsx, vendor/account/page.tsx | Interface PayoutMethod + active_channel/paypal_email. account : handleSaveMethod passe channel:'mobile_money' puis re-fetch | Local (non déployé) | UI | Automatique | Étendre l'interface ; MAJ handleSaveMethod |
| 2026-09-03 | DIARRA | frontend/src/app/(app)/vendor/earnings/page.tsx | Carte « Mobile Money » (renommée) + nouvelle carte « PayPal » (email, prioritaire). `hasPayoutMethod` = mobile money OU PayPal ; PayPal dispense de la vérif téléphone. Tiroir de retrait : « Envoyé sur » affiche PayPal + email si canal PayPal | Local (non déployé) | UI (paiement) | Automatique | Séparer les 2 cartes, états paypal*, handleSavePayPal, hasPayPal/hasMobileMoney |
| 2026-09-03 | DIARRA | frontend/src/app/(app)/admin/payouts/page.tsx | `paymentMethodOf` : renvoie {label:'PayPal', phone:email} si canal PayPal. Badge « Voie » PayPal. Bouton « Régler via PayPal ». ConfirmDialog auto : description adaptée (email + mention conversion USD) | Local (non déployé) | UI (paiement) | Automatique | Étendre l'interface Payout + paymentMethodOf + badge + libellé bouton + description dialog |
| 2026-09-03 | DIARRA | README.md | Section « Domaines de production » réécrite selon l'archi réelle (constatée dans worker/wrangler.jsonc + k8s) : site = diarra.app (Cloudflare Worker), API/WS/webhooks = api.diarra.app (backend Go VPS), stockage = files.diarra.app. .env d'exemple : FRONTEND_URL/CORS = diarra.app, NEXT_PUBLIC_API_URL/WS = api.diarra.app, PAWAPAY_CALLBACK_URL + bloc PAYPAL_* → api.diarra.app/api/webhooks/*. Caddyfile d'exemple : bloc `api.diarra.app` (au lieu de diarra.abmcy.com), le frontend n'est plus servi par Caddy. Curl de vérif + URLs actuelles mis à jour | Local (non déployé) | Documentation | Automatique | Remplacer diarra.abmcy.com par diarra.app/api.diarra.app, s3.diarra.abmcy.com par files.diarra.app |
| 2026-09-03 | DIARRA | docker-compose.yml, frontend/.env.example | `NEXT_PUBLIC_SITE_URL` par défaut : diarra.abmcy.com → diarra.app | Local (non déployé) | Config | Automatique | sed sur la valeur par défaut |
| 2026-09-03 | DIARRA | k8s/ingress.yaml | Host `diarra.abmcy.com` → `api.diarra.app` ; suppression du host abandonné `origin.abmcy.com` (526), remplacé par un path `/` sur api.diarra.app (health + webhooks directs). Commentaire d'en-tête réécrit (Worker diaara + BACKEND_URL api.diarra.app) | Local (non déployé) | Déploiement (k8s) | Automatique | Renommer le host, fusionner le path racine, MAJ commentaire |
| 2026-09-03 | DIARRA | k8s/configmap.yaml | `S3_ENDPOINT` s3.diarra.abmcy.com → files.diarra.app. Commentaire FRONTEND_URL réécrit. Ajout `PAYPAL_BASE_URL` (défaut live) | Local (non déployé) | Déploiement (k8s) | Automatique | sed S3_ENDPOINT + ajout de la clé PAYPAL_BASE_URL |
| 2026-09-03 | DIARRA | k8s/create-secret.sh, k8s/minio.yaml | create-secret.sh : ajout PAYPAL_CLIENT_ID/CLIENT_SECRET/BASE_URL/WEBHOOK_ID au Secret diarra-secrets (lus depuis le .env du VPS). minio.yaml : commentaire s3.diarra.abmcy.com → files.diarra.app | Local (non déployé) | Déploiement (k8s) | Automatique | Ajouter les 4 --from-literal PAYPAL_* avant FIREBASE_PROJECT_ID ; sed sur le commentaire minio |
| 2026-09-03 | DIARRA | backend/.env.example, README.md | Nettoyage : FRONTEND_URL/S3_ENDPOINT/PAWAPAY_CALLBACK_URL d'exemple pointent vers diarra.app / files.diarra.app / api.diarra.app. Bloc PAYPAL_* documenté avec l'URL webhook api.diarra.app | Local (non déployé) | Documentation | Automatique | Remplacer les URLs d'exemple |
